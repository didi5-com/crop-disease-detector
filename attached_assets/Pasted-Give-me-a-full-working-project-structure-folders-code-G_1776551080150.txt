Give me a full working project structure (folders + code)

Good—this is where most people either stay theoretical or actually build something usable. I’ll give you a clean, production-style project structure + working Python code you can run immediately, then you can iterate.

🧠 Project Overview

You’re building:

Image upload system
CNN model (transfer learning)
Disease prediction
Web interface (Flask)
📁 Full Project Structure
crop_disease_detector/
│
├── app.py
├── train_model.py
├── predict.py
├── requirements.txt
│
├── model/
│   └── model.h5
│
├── static/
│   └── uploads/
│
├── templates/
│   ├── index.html
│   └── result.html
│
├── dataset/
│   ├── train/
│   ├── val/
│   └── test/
│
└── utils/
    └── preprocessing.py
⚙️ 1. Install Dependencies
requirements.txt
flask
tensorflow
numpy
opencv-python
pillow

Install:

pip install -r requirements.txt
🧪 2. Image Preprocessing
utils/preprocessing.py
import cv2
import numpy as np

def preprocess_image(image_path):
    img = cv2.imread(image_path)
    img = cv2.resize(img, (224, 224))
    img = img / 255.0
    img = np.reshape(img, (1, 224, 224, 3))
    return img
🤖 3. Train the Model
train_model.py
from tensorflow.keras.preprocessing.image import ImageDataGenerator
from tensorflow.keras.applications import MobileNetV2
from tensorflow.keras.layers import Dense, Flatten
from tensorflow.keras.models import Model

train_dir = "dataset/train"
val_dir = "dataset/val"

train_gen = ImageDataGenerator(rescale=1./255)
val_gen = ImageDataGenerator(rescale=1./255)

train_data = train_gen.flow_from_directory(train_dir, target_size=(224,224), batch_size=32, class_mode='categorical')
val_data = val_gen.flow_from_directory(val_dir, target_size=(224,224), batch_size=32, class_mode='categorical')

base_model = MobileNetV2(weights='imagenet', include_top=False, input_shape=(224,224,3))

x = base_model.output
x = Flatten()(x)
x = Dense(128, activation='relu')(x)
predictions = Dense(train_data.num_classes, activation='softmax')(x)

model = Model(inputs=base_model.input, outputs=predictions)

model.compile(optimizer='adam', loss='categorical_crossentropy', metrics=['accuracy'])

model.fit(train_data, validation_data=val_data, epochs=5)

model.save("model/model.h5")
🔍 4. Prediction Logic
predict.py
import numpy as np
from tensorflow.keras.models import load_model
from utils.preprocessing import preprocess_image

model = load_model("model/model.h5")

class_labels = ['Healthy', 'Disease1', 'Disease2', 'Disease3']

def predict_disease(image_path):
    img = preprocess_image(image_path)
    prediction = model.predict(img)
    
    class_index = np.argmax(prediction)
    confidence = float(np.max(prediction))
    
    return class_labels[class_index], confidence
🌐 5. Flask Web App
app.py
from flask import Flask, render_template, request
import os
from predict import predict_disease

app = Flask(__name__)
UPLOAD_FOLDER = 'static/uploads'
app.config['UPLOAD_FOLDER'] = UPLOAD_FOLDER

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/predict', methods=['POST'])
def predict():
    if 'file' not in request.files:
        return "No file uploaded"

    file = request.files['file']
    filepath = os.path.join(app.config['UPLOAD_FOLDER'], file.filename)
    file.save(filepath)

    label, confidence = predict_disease(filepath)

    return render_template('result.html', label=label, confidence=round(confidence*100, 2), image=filepath)

if __name__ == '__main__':
    app.run(debug=True)
🖥️ 6. Frontend UI
templates/index.html
<!DOCTYPE html>
<html>
<head>
    <title>Crop Disease Detector</title>
</head>
<body>
    <h2>Upload Leaf Image</h2>
    <form action="/predict" method="post" enctype="multipart/form-data">
        <input type="file" name="file" required>
        <button type="submit">Detect</button>
    </form>
</body>
</html>
templates/result.html
<!DOCTYPE html>
<html>
<head>
    <title>Result</title>
</head>
<body>
    <h2>Prediction Result</h2>
    
    <img src="{{ image }}" width="300"><br><br>
    
    <h3>Disease: {{ label }}</h3>
    <h3>Confidence: {{ confidence }}%</h3>

    <a href="/">Try Another</a>
</body>
</html>
📦 7. Dataset Format (IMPORTANT)

Your dataset must look like this:

dataset/train/
    Healthy/
    Disease1/
    Disease2/

dataset/val/
    Healthy/
    Disease1/
    Disease2/

Each folder contains images.

▶️ 8. Run the System
Step 1: Train model
python train_model.py
Step 2: Start app
python app.py

Open:

http://127.0.0.1:5000
🚀 What You Should Improve Next

Right now this is functional but basic.

Upgrade it by:

Adding real disease names (e.g., “Cassava Mosaic Virus”)
Adding treatment suggestions
Using better dataset (real farm images)
Improving UI (you like cyberpunk design — apply it here)
Converting to mobile (TensorFlow Lite)
⚠️ Reality Check
Accuracy depends 90% on dataset quality
Nigerian farm images ≠ lab dataset → expect drop in accuracy
You’ll need to fine-tune with local data later